import { FormatadataDirective } from './formatadata.directive';

describe('FormatadataDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatadataDirective();
    expect(directive).toBeTruthy();
  });
});
